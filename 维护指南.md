# 个人主页维护指南

## 30 秒定位

日常更新只需要记住 4 个文件：

| 需求                             | 编辑文件                   |
| -------------------------------- | -------------------------- |
| 发布“论文上线、录用、实习”等动态 | `_data/updates.yml`        |
| 新增/修改论文                    | `_bibliography/papers.bib` |
| 修改学术简历并生成 PDF           | `_data/cv.yml`             |
| 修改首页自我介绍                 | `_pages/about.md`          |

主页视觉和交互已经模板化，常规增删改不需要复制 HTML。

## 首页个人描述

首页可见文案集中在 `_pages/about.md`，按下面四层修改：

| 位置                           | 作用                       | 建议                                         |
| ------------------------------ | -------------------------- | -------------------------------------------- |
| front matter 的 `subtitle`     | 姓名下方的身份、学校与导师 | 保持一行，只放当前身份                       |
| `eyebrow` 段落                 | 头像旁的研究定位短句       | 使用 3 个以内的关键词                        |
| 紧随其后的两个 Markdown 段落   | 个人描述正文               | 第一段讲核心研究问题，第二段讲当前方向与背景 |
| `research-strip` 中的 `<span>` | 首页研究标签               | 保留 4–6 个稳定方向                          |

`_config.yml` 中的 `description` 主要用于页面 metadata、RSS 和搜索摘要，并
不会替代首页正文；修改个人定位时应同步检查，但不要把长段落复制进去。

首页的 Experience、Education 与 Honors 摘要由 `_data/cv.yml` 自动读取，
不在 `_pages/about.md` 重复维护；首页只显示每类前两条，完整内容仍在 CV 页面。

## Updates / Activities

最新内容永远放在 `_data/updates.yml` 顶部：

```yaml
- date: 2026-08-01
  label: Paper
  text: "Released our new paper on ..."
  url: https://arxiv.org/abs/xxxx.xxxxx
  link_text: arXiv
```

- `date`：使用 `YYYY-MM-DD`。
- `label`：建议使用 `Paper`、`Preprint`、`Talk`、`Experience`、`Award`。
- `text`：一到两句话，只写可核验事实。
- `url`、`link_text`：可选。

首页会自动生成可手动切换、可暂停的纵向更新流；系统设置“减少动态效果”
时会自动停止滚动。

Updates 的可视高度在 `_sass/_components.scss` 的 `.updates-viewport` 中设置。
当前 `max-height: 20.25rem`，约显示三条标准高度的动态；动态仍全部保留并可滚动。

## 姓名与浏览器图标

- 导航栏和首页姓名由 `_config.yml` 的 `first_name` / `last_name` 提供；模板已使用
  统一字重，不再只加粗 `first_name`。
- 浏览器标签页图标由 `_config.yml` 的 `icon` 指向
  `assets/img/favicon.svg`。替换 SVG 时保持正方形 `viewBox` 和小尺寸可辨识性。

## Publications

在 `_bibliography/papers.bib` 增加 BibTeX 条目：

- `selected = {true}`：同时显示在首页；
- `selected = {false}`：只显示在 Publications 页面；
- `abbr`：venue 短名，如 `ACL`、`AAAI`、`arXiv`；
- `booktitle`（会议）或 `journal`（期刊/预印本）：页面正文展示的完整 venue；
- `year`：与完整 venue 独立显示，窄屏下自动换到下一行；
- `pdf`、`code`、`website`、`video`：生成对应按钮。

不要手工编辑生成后的 Publications HTML。`_pages/publications.md` 的
`<div class="publications">` 是论文样式与 BibTeX 折叠行为的作用域，不要删除。

## CV

网页 `/cv/` 不再从 `_data/cv.yml` 重构 HTML，而是直接嵌入
`assets/pdf/Kuang_Wang_CV_en.pdf` 或 `assets/pdf/Kuang_Wang_CV_zh.pdf`。
页面默认显示英文版，语言按钮只切换嵌入资源；“Open PDF” 始终打开当前语言的
原始 PDF。

两份 PDF 的权威源码在
[`phd_core/05_personal_brand_and_skills/resume`](https://github.com/wangkevin02/phd_core/tree/main/05_personal_brand_and_skills/resume)；
本地 monorepo 中对应 `../resume/`。英文与中文使用同一套单页 LaTeX 模板；
中文版只保留翻译、CJK 字体、标点和日期表达差异。生成、校验与同步命令：

```bash
cd ../resume
./build_resume.sh pdf all
cd ../wangkevin02.github.io
cp ../resume/en_resume.pdf assets/pdf/Kuang_Wang_CV_en.pdf
cp ../resume/zh_resume.pdf assets/pdf/Kuang_Wang_CV_zh.pdf
```

`_data/cv.yml` 仍为首页 Experience / Education / Honors 摘要的数据源，不再负责
`/cv/` 的正文渲染。

## 本地预览与发布

```bash
docker compose up --build
```

浏览器打开 `http://localhost:8080`。推送到 `main` 后，GitHub Actions 会构建并
发布站点。修改 LaTeX 简历后，需要先重新生成并复制两份 PDF 再提交。

发布前至少检查：

1. 手机与桌面首屏；
2. 明亮/暗色模式；
3. Updates 的前后切换、暂停和键盘焦点；
4. 论文链接、CV 的中英文切换与 Open PDF 回退；
5. 页面中是否出现示例人物、虚构书籍或模板占位文字。
